filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/test.log

# Forward logs to Fluent Bit over HTTP
output.http:
  url: "http://145.223.73.4:5044"
  max_retries: 0
  timeout: 30

logging.level: error
logging.to_files: false
path.data: /var/lib/filebeat
path.logs: /var/log/filebeat
